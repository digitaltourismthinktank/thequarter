/* Placeholder home — replaced by the real homepage in a later step.
   Present only so the scaffold builds and runs. */
export default function HomePage() {
  return (
    <main id="main" style={{ maxWidth: 760, margin: '0 auto', padding: '120px 32px' }}>
      <span className="q-eyebrow">Cathedral Quarter, Canterbury</span>
      <h1 className="q-display" style={{ fontSize: 'clamp(40px, 6vw, 72px)', marginTop: 14 }}>
        So much more than a workspace
      </h1>
      <p style={{ fontSize: 18, lineHeight: 1.6, color: 'var(--text-body)', marginTop: 18 }}>
        The Quarter — front-end build in progress.
      </p>
    </main>
  );
}
