# dependencies
/node_modules

# local Node toolchain (project-contained, not committed)
/.tooling

# next.js
/.next
/out

# netlify
/.netlify

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files (Stripe / Typeform keys live here later)
.env
.env*.local

# editor / agent session state
/.claude

# design-system handoff: keep source, drop the unused full-res originals
the-quarter-design-system/project/uploads/
