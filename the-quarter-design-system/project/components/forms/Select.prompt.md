Styled native select. Pass `options` as strings or `{value,label}`.

```jsx
<Select label="Party size" options={['2–4 people','5–8 people','8–10 people']} />
<Select label="Duration" options={[{value:'half',label:'Half day'},{value:'full',label:'Full day'}]} />
```
