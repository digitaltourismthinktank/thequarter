Calm dashboard metric tile. Optional `progress` bar and `icon`. Tones: `default`, `gold`, `ink`.

```jsx
<StatTile label="Days remaining" value="6" unit="of 10" icon="calendar" progress={60} hint="Resets 1 Jul" />
<StatTile label="Plan" value="Resident" icon="user" tone="gold" />
```
