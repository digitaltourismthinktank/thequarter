Calm, warm empty state — dashed soft panel, gold icon chip, optional CTA. Voice stays conversational.

```jsx
<EmptyState icon="calendar" title="No bookings yet"
  message="When you reserve a room or desk, it'll show up here."
  actionLabel="Find a room" onAction={book} />
```
